name: currency_exchange

version: 1.0.0

config-version: 2

profile: default

target-path: "target"  # Directory for compiled files
clean-targets:
  - "target"
  - "dbt_packages"

models:
  currency_exchange:  
    dwh:
      database: analytics
      materialized: incremental 
    avg_exchange_rate_trends_vw:  
      +materialized: view  